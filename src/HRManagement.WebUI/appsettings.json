{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // WebUI → API çağrılarının hedefi. Sunucudan sunucuya yapılır, tarayıcı görmez.
  "ApiSettings": {
    "BaseUrl": "http://localhost:5100/"

    // "ApiKey" bilerek BURADA DEĞİL: sırdır, git'e girmez. user-secrets ile verilir
    // ve API'nin "ApiKey" değeriyle AYNI olmalıdır (paylaşılan sır).
  }
}
