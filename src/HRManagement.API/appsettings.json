{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "SeedAdmin": {
    "Username": "admin",
    "Email": "admin@hrmanagement.local"
  },
  "Jwt": {
    "Issuer": "HRManagement.API",
    "Audience": "HRManagement.WebUI"
  }

  // Jwt:Key ve ApiKey bilerek burada değil: ikisi de sırdır, user-secrets ile verilir.
  // ApiKey, WebUI'daki ApiSettings:ApiKey ile aynı değer olmalıdır.
}
