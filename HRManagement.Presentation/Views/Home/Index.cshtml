@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="page-head">
    <h1>Hoş geldiniz 👋</h1>
    <p class="text-muted mb-0">Rolünüze göre özet kartlar ve listeler burada görünecek.</p>
</div>

@*
    ── Rol bazlı dashboard planı (Gereksinim 5.5) ─────────────────────────
    Auth bağlandığında bu bölüm User.IsInRole(...) ile ayrışacak:
      HR       → toplam aktif çalışan, bekleyen izin talebi, aktif stajyer
      Yönetici → ekip çalışan sayısı, ekipten bekleyen izin talepleri
      Çalışan  → kalan yıllık izin, son 5 izin talebi
    Şimdilik hepsi için yer tutucular duruyor.
*@

<div class="row g-3 mt-1">
    <div class="col-6 col-xl-3">
        <div class="stat-card placeholder">İstatistik kartı</div>
    </div>
    <div class="col-6 col-xl-3">
        <div class="stat-card placeholder">İstatistik kartı</div>
    </div>
    <div class="col-6 col-xl-3">
        <div class="stat-card placeholder">İstatistik kartı</div>
    </div>
    <div class="col-6 col-xl-3">
        <div class="stat-card placeholder">İstatistik kartı</div>
    </div>
</div>

<div class="row g-3 mt-1">
    <div class="col-lg-7">
        <div class="panel">
            <div class="panel-head">Son İzin Talepleri</div>
            <div class="panel-body placeholder">İçerik sonra doldurulacak</div>
        </div>
    </div>
    <div class="col-lg-5">
        <div class="panel">
            <div class="panel-head">Hızlı İşlemler</div>
            <div class="panel-body placeholder">İçerik sonra doldurulacak</div>
        </div>
    </div>
</div>
